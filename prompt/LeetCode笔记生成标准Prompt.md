# LeetCode笔记生成标准Prompt

> **用途**: 为LeetCode题目生成高质量学习笔记，遵循ID 106的优秀格式标准
> **目标**: 3个月后仅靠笔记能写出AC代码

---

## Prompt模板

```
你是一位资深算法工程师，擅长编写高质量的LeetCode题目笔记。

# 任务
为以下LeetCode题目生成高质量的学习笔记，严格遵循格式和质量标准。

# 题目信息
- **题号**: {leetcode_number}
- **标题**: {title}
- **难度**: {difficulty}
- **标签**: {tags}

## 题目描述
{question_description}

## 答案要求
{answer_requirements}

---

# 笔记格式要求（基于优秀样本ID 106）

你必须生成以下章节，每个章节都要详实：

## 1. 解题思路（必需）

**要求**：
- 列举2-3种可能的解法
- 对比各方法的优劣（时间/空间复杂度）
- 明确推荐哪种方法，并说明原因

**示例格式**：
```
本题要求[问题描述]。主要有三种经典解法：

1. **方法一名称（推荐）**：简要描述，时间O(?)，空间O(?)
2. **方法二名称**：简要描述，时间O(?)，空间O(?)
3. **方法三名称**：简要描述（可选）

推荐使用**方法一**，因为：
- 原因1
- 原因2
```

## 2. 解题步骤（必需）

**要求**：
- 为每种方法提供详细步骤（3-7步）
- 步骤要具体到"遍历什么"、"检查什么条件"、"如何更新变量"
- 不要写空洞的步骤如"分析题目"、"实现算法"

**示例格式**：
```
### 方法一：中心扩展法（推荐）

1. **遍历每个可能的中心**：对字符串的每个位置i，将其作为潜在的回文中心
2. **考虑两种情况**：
   - 奇数长度回文：以单个字符为中心 (i, i)
   - 偶数长度回文：以两个字符之间为中心 (i, i+1)
3. **中心扩展**：从中心向两边扩展，只要两边字符相等就继续扩展
4. **记录最长回文**：用start和end记录当前找到的最长回文的起止位置
5. **返回结果**：使用substring(start, end+1)返回最长回文子串

### 方法二：动态规划法

1. **定义状态**：dp[i][j] 表示 s[i...j] 是否为回文
2. [继续...]
```

## 3. 代码示例（必需）

**要求**：
- 提供完整可运行的Java代码
- 必须包含关键注释
- 如果有多种方法，都要提供代码
- 代码要处理边界条件
- 辅助函数要单独实现（如中心扩展的helper）

**示例格式**：
```java
### 中心扩展法（推荐）

\```java
public class Solution {
    public String longestPalindrome(String s) {
        if (s == null || s.length() < 1) return "";

        int start = 0, end = 0;

        for (int i = 0; i < s.length(); i++) {
            // 奇数长度回文：以 i 为中心
            int len1 = expandAroundCenter(s, i, i);
            // 偶数长度回文：以 i 和 i+1 之间为中心
            int len2 = expandAroundCenter(s, i, i + 1);

            int len = Math.max(len1, len2);

            // 更新最长回文的起始和结束位置
            if (len > end - start) {
                start = i - (len - 1) / 2;
                end = i + len / 2;
            }
        }

        return s.substring(start, end + 1);
    }

    /**
     * 从中心向两边扩展，返回最长回文长度
     */
    private int expandAroundCenter(String s, int left, int right) {
        while (left >= 0 && right < s.length() &&
               s.charAt(left) == s.charAt(right)) {
            left--;
            right++;
        }
        // 返回回文长度
        return right - left - 1;
    }
}
\```

### 动态规划法

\```java
[完整代码...]
\```
```

## 4. 关键提示（必需）

**要求**：
- 至少3-5条具体提示
- 包括：边界条件、易错点、优化思路、相关题目
- 避免空洞的提示如"注意边界情况"，要说明是什么边界

**示例格式**：
```
1. **边界条件处理**
   - 空字符串和单字符直接返回
   - 中心扩展时注意数组越界：left >= 0 && right < s.length()

2. **中心扩展的两种情况**
   - 奇数长度回文：中心是单个字符 (i, i)
   - 偶数长度回文：中心是两个字符之间 (i, i+1)
   - **容易遗漏偶数情况**，导致 "cbbd" 无法正确返回 "bb"

3. **索引计算技巧**
   - 从回文长度len和中心位置i计算起始位置：
     start = i - (len - 1) / 2
     end = i + len / 2
   - 这个公式对奇数和偶数长度都适用

4. **优化思路**
   - 剪枝：如果剩余字符数不足以产生更长回文，可以提前终止
   - 进阶：Manacher算法可以达到O(n)时间复杂度

5. **相关题目**
   - LeetCode 647: Palindromic Substrings（回文子串计数）
   - LeetCode 516: Longest Palindromic Subsequence（注意区别）
```

## 5. 复杂度分析（推荐）

**要求**：
- 分析每种方法的时间和空间复杂度
- 简要说明复杂度的来源

**示例格式**：
```
### 中心扩展法
- **时间复杂度**: O(n²) - 遍历n个中心，每个中心最多扩展n次
- **空间复杂度**: O(1) - 只用了常数个变量

### 动态规划法
- **时间复杂度**: O(n²) - 两层循环填表
- **空间复杂度**: O(n²) - 需要n×n的DP表
```

## 6. 核心策略（必需，单独一行）

**要求**：
- 一句话总结算法本质（不超过30字）
- 这句话将存入数据库的 `core_strategy` 字段
- 要能快速回忆起整个算法

**示例格式**：
```
**核心策略**: 以每个位置为中心（奇数/偶数两种情况），向两边扩展找最长回文
```

---

# 质量标准（必须达到）

✅ **字数要求**: 800字以上
✅ **代码要求**: 40行以上（不含空行和注释）
✅ **结构完整**: 包含全部6个章节
✅ **测试标准**: 3个月后仅靠笔记能写出AC代码

## 自检清单

生成笔记前，确保：
- [ ] 解题思路列举了2-3种方法？
- [ ] 每种方法都有详细步骤（3-7步）？
- [ ] 代码完整可运行，包含注释？
- [ ] 关键提示至少3条，且具体？
- [ ] 核心策略不超过30字？
- [ ] 总字数超过800字？
- [ ] 代码行数超过40行？

---

# 输出要求

1. **直接输出Markdown格式的笔记**，不要有额外的解释
2. **严格遵循上述6个章节的结构**
3. **确保内容详实、代码完整、步骤具体**
4. **在最后单独生成"核心策略"一行**（格式：`**核心策略**: [一句话总结]`）

---

# 优秀参考样本（ID 106）

以下是一个优秀笔记的完整示例，供参考：

[见 notes/5_Longest_Palindromic_Substring.md]

---

# 开始生成

现在请为上述题目生成笔记！
```

---

## 使用方式

### 方式1：直接使用（推荐）

1. 用户提供题号（如："生成209号题的笔记"）
2. Claude从数据库读取题目信息
3. 将题目信息填入上述Prompt模板的占位符
4. 生成笔记并保存到 `notes/` 目录

### 方式2：手动填充

如果题目不在数据库中，可以手动填充：

```
你是一位资深算法工程师，擅长编写高质量的LeetCode题目笔记。

# 任务
为以下LeetCode题目生成高质量的学习笔记，严格遵循格式和质量标准。

# 题目信息
- **题号**: 209
- **标题**: Minimum Size Subarray Sum
- **难度**: MEDIUM
- **标签**: Array, Binary Search, Sliding Window

## 题目描述
Given an array of positive integers nums and a positive integer target,
return the minimal length of a subarray whose sum is greater than or equal to target.

[... 使用上述完整Prompt模板 ...]
```

---

## 版本历史

- **v1.0** (2025-12-24): 基于ID 106创建初始版本，包含6个章节和质量标准
- **未来改进**:
  - [ ] 添加图片生成提示（算法示意图）
  - [ ] 支持多语言代码（Python, C++）
  - [ ] 添加难度分级的差异化要求

---

## 附录：常见算法模式的核心策略示例

供参考，帮助提炼"核心策略"：

| 题目类型 | 核心策略示例 |
|---------|------------|
| 回文问题 | 以每个位置为中心向两边扩展 |
| 滑动窗口 | 右指针扩展满足条件，左指针收缩寻找最优解 |
| 二分查找 | 缩小搜索范围，每次排除一半不可能的解 |
| 动态规划 | 定义状态，找出状态转移方程，自底向上填表 |
| DFS/BFS | 遍历所有可能路径/层级，标记已访问避免重复 |
| 双指针 | 左右指针相向移动，根据条件决定移动方向 |
| 贪心算法 | 每步选择局部最优，最终达到全局最优 |

---

**最后提醒**: 生成笔记时，务必确保代码可以直接运行（复制粘贴到IDE即可AC），这是质量的核心标准！
